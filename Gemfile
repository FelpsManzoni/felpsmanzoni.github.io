source "https://rubygems.org"

# Use the GitHub Pages dependency set (matches GitHub's build exactly)
gem "github-pages", group: :jekyll_plugins

# Needed when running Jekyll on Ruby >= 3.0
gem "webrick", "~> 1.8"
